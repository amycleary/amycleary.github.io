doctype html
html(lang="en")
  head
    != partial("_meta")
  body
    input.nav-trigger.psuedo-hidden(type="checkbox", id="nav-trigger")
    label(for="nav-trigger")
    .canvas
      .container.header
        != partial("_header")
        != partial("_navbar")
      .container.main
        main(role="main")
          != yield
        != partial("_footer")
